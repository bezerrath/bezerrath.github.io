import { createStore } from 'vuex'
import pokemons from '@/store/modules/pokemons'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    pokemons
  }
})
